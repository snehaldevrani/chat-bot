[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: Full pipeline integration tests (real graph, DB, guardrails; LLM mocked)
    load: Concurrent load tests measuring throughput and latency under stress
